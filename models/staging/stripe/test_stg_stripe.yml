version: 2

models:
    - name: stg_payments
      columns: 
        # - name: order_id
        #   tests:
        #     - unique
        #     - not_null
        - name: status
          tests: 
            - accepted_values:
                values:
                    - success
                    - fail
        - name: payment_method
          tests:
            - accepted_values: 
                values:
                    - credit_card
                    - coupon
                    - bank_transfer
                    - gift_card

